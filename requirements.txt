# Core
openai>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Gemini provider
google-genai>=2.9.0

# Linting & Code Quality (Python)
flake8>=6.0.0
ruff>=0.4.0

# Observability
structlog>=24.0.0
rich>=13.0.0

# Retry / Resilience
tenacity>=8.2.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-mock>=3.12.0

# Type Checking
mypy>=1.9.0

# ── System tools (pip 외 별도 설치 필요) ──────────────────────────────────────
# Bash  : shellcheck  →  apt install shellcheck  /  brew install shellcheck
# C/C++ : gcc, g++    →  apt install build-essential
# C/C++ : clang-tidy  →  apt install clang-tidy  /  brew install llvm
