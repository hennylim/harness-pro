[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --tb=short
    -q
    --cov=agent
    --cov-report=term-missing
    --cov-fail-under=50
